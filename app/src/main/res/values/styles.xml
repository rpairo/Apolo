<resources>

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Declaración de los colores para el estilo Material Design -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>

        <item name="android:windowBackground">@color/window_background</item>

    </style>

    <style name="Theme.Apolo" parent="Base.AppTheme" />

    <style name="Theme.NavigationDrawer" parent="Base.AppTheme" />

    <style name="Base.NavigationViewTheme" parent="Theme.NavigationDrawer">
        <item name="textAppearanceListItem">@style/TextAppearance.AppCompat.Body2</item>
    </style>

    <style name="Theme.NavigationViewTheme" parent="Base.NavigationViewTheme" />

    <style name="Base.CustomActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>

        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <style name="Theme.CustomActionBar" parent="Base.CustomActionBar" />


    <style name="Base.CardContent" parent="android:Widget">
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">24dp</item>
        <item name="android:paddingBottom">24dp</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="Theme.CardContent" parent="Base.CardContent" />

</resources>